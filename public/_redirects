# Cloudflare Pages redirects - manejo correcto de 404

# Archivos estáticos y recursos
/_astro/* /_astro/:splat 200
/favicon.ico /favicon.ico 200
/robots.txt /robots.txt 200
/sitemap.xml /sitemap.xml 200
/wp-content/* /wp-content/:splat 200
/images/* /images/:splat 200
/logos/* /logos/:splat 200

# SPA fallback - páginas que no existen deben mostrar 404
/* /404.html 404
