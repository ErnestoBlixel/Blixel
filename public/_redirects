# Cloudflare Pages redirects para manejo correcto de 404

# SPA fallback - páginas que no existen deben mostrar 404
/* /404 404

# Redirecciones específicas si las necesitas en el futuro
# /old-page /new-page 301

# Manejo de archivos estáticos
/_astro/* /_astro/:splat 200
/favicon.ico /favicon.ico 200
/robots.txt /robots.txt 200
/sitemap.xml /sitemap.xml 200
