---
title: "Magic UI Notifications Demo"
description: "Demostración de las tarjetas de notificación Magic UI para Blixel AI"
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <style>
      body {
        font-family: 'Inter', sans-serif;
      }
    </style>
  </head>
  
  <body>
    <div id="magic-notifications-root"></div>
    
    <script>
      // Configurar Tailwind para modo dark por defecto
      document.documentElement.classList.add('dark');
    </script>
  </body>
</html>

<script>
  import MagicNotificationsDemo from '../components/MagicNotificationsDemo.tsx';
  import { createRoot } from 'react-dom/client';
  import React from 'react';

  // Renderizar el componente React
  const container = document.getElementById('magic-notifications-root');
  if (container) {
    const root = createRoot(container);
    root.render(React.createElement(MagicNotificationsDemo));
  }
</script>
