---
// Redirigir a la página principal con el idioma catalán detectado automáticamente
export const prerender = false;

// Esta página simplemente importa la página principal
// El sistema de i18n detectará automáticamente que estamos en /ca/ y mostrará el contenido en catalán
---

<script>
  // Redirección inmediata a la página principal con detección de idioma
  if (typeof window !== 'undefined') {
    // Si estamos en /ca/, redirigir a /ca 
    const currentPath = window.location.pathname;
    if (currentPath === '/ca/index.astro' || currentPath === '/ca/') {
      window.location.replace('/ca');
    }
  }
</script>

<!-- 
  Esta página se mantiene por compatibilidad, pero ahora usamos un sistema unificado.
  La página principal (index.astro) detecta automáticamente el idioma desde la URL
  y muestra el contenido correspondiente usando el sistema de traducciones.
  
  Para agregar contenido específico de catalán, usa las funciones de traducción
  en los archivos JSON correspondientes.
-->

<style>
  /* Página de redirección temporal - sin estilos necesarios */
  body {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background: #000;
    color: #fff;
    font-family: system-ui;
  }
  
  .loading {
    text-align: center;
  }
  
  html:not(.dark) body {
    background: #fff;
    color: #000;
  }
</style>

<div class="loading">
  <p>Carregant / Cargando...</p>
</div>