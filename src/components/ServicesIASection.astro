---
// Sección: Soluciones de IA para Empresas que ofrecemos
---

<section id="servicios" class="services-ia-section">
  <div class="container">
    <div class="services-header">
      <div class="section-badge">
        <span>Nuestras Soluciones</span>
      </div>
      
      <h2 class="services-title">
        Soluciones de <span class="gradient-text">IA para Empresas</span> que ofrecemos
      </h2>
      
      <p class="services-description">
        Implementamos tecnología de inteligencia artificial adaptada a las necesidades específicas de tu sector empresarial.
      </p>
    </div>

    <div class="services-grid">
      <!-- Formación IA Empresas -->
      <div class="service-card" data-service="formacion">
        <div class="card-header">
          <div class="service-icon">
            <div class="icon-background">🎓</div>
          </div>
          <h3>Formación IA Empresas</h3>
        </div>
        
        <div class="card-content">
          <p>Capacitamos a tus equipos en el uso estratégico de herramientas de IA para maximizar la productividad empresarial.</p>
          
          <ul class="service-features">
            <li>
              <div class="feature-icon">✓</div>
              <span>Workshops personalizados por departamento</span>
            </li>
            <li>
              <div class="feature-icon">✓</div>
              <span>ChatGPT y herramientas IA aplicadas</span>
            </li>
            <li>
              <div class="feature-icon">✓</div>
              <span>Casos de uso específicos de tu sector</span>
            </li>
            <li>
              <div class="feature-icon">✓</div>
              <span>Certificación en IA Empresarial</span>
            </li>
          </ul>
        </div>
        
        <div class="card-footer">
          <a href="/formacion-ia-empresas" class="service-cta">
            <span>Ver Detalle</span>
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
        </div>
      </div>

      <!-- Agentes IA y ChatGPT Empresas -->
      <div class="service-card featured" data-service="agentes">
        <div class="card-header">
          <div class="service-icon">
            <div class="icon-background">🤖</div>
          </div>
          <h3>Agentes IA y ChatGPT Empresas</h3>
          <div class="featured-badge">Más Popular</div>
        </div>
        
        <div class="card-content">
          <p>Desarrollamos agentes de inteligencia artificial personalizados que automatizan procesos específicos de tu empresa.</p>
          
          <ul class="service-features">
            <li>
              <div class="feature-icon">✓</div>
              <span>Chatbots empresariales inteligentes</span>
            </li>
            <li>
              <div class="feature-icon">✓</div>
              <span>Automatización de atención al cliente</span>
            </li>
            <li>
              <div class="feature-icon">✓</div>
              <span>Asistentes virtuales para RRHH</span>
            </li>
            <li>
              <div class="feature-icon">✓</div>
              <span>Integración con sistemas existentes</span>
            </li>
          </ul>
        </div>
        
        <div class="card-footer">
          <a href="/agentes-ia-empresas" class="service-cta">
            <span>Ver Detalle</span>
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
        </div>
      </div>

      <!-- Automatización Inteligente & micro-SaaS -->
      <div class="service-card" data-service="automatizacion">
        <div class="card-header">
          <div class="service-icon">
            <div class="icon-background">⚙️</div>
          </div>
          <h3>Automatización Inteligente & micro-SaaS</h3>
        </div>
        
        <div class="card-content">
          <p>Creamos soluciones SaaS a medida que automatizan procesos repetitivos y optimizan flujos de trabajo empresariales.</p>
          
          <ul class="service-features">
            <li>
              <div class="feature-icon">✓</div>
              <span>Automatización de procesos (RPA)</span>
            </li>
            <li>
              <div class="feature-icon">✓</div>
              <span>Micro-SaaS personalizados</span>
            </li>
            <li>
              <div class="feature-icon">✓</div>
              <span>Integración con APIs empresariales</span>
            </li>
            <li>
              <div class="feature-icon">✓</div>
              <span>Dashboards inteligentes en tiempo real</span>
            </li>
          </ul>
        </div>
        
        <div class="card-footer">
          <a href="/automatizacion-ia-empresas" class="service-cta">
            <span>Ver Detalle</span>
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
        </div>
      </div>
    </div>

    <!-- Departamentos que atendemos -->
    <div class="departments-section">
      <h3 class="departments-title">IA para cada departamento de tu empresa</h3>
      <div class="departments-grid">
        <div class="department-item">
          <div class="dept-icon">💼</div>
          <span>IA para Marketing</span>
        </div>
        <div class="department-item">
          <div class="dept-icon">💰</div>
          <span>IA para Finanzas</span>
        </div>
        <div class="department-item">
          <div class="dept-icon">👥</div>
          <span>IA para RRHH</span>
        </div>
        <div class="department-item">
          <div class="dept-icon">📊</div>
          <span>IA para Operaciones</span>
        </div>
        <div class="department-item">
          <div class="dept-icon">🛒</div>
          <span>IA para Ventas</span>
        </div>
        <div class="department-item">
          <div class="dept-icon">🎯</div>
          <span>IA para Estrategia</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .services-ia-section {
    padding: 6rem 0;
    background: #171717;
    position: relative;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .services-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(96, 165, 250, 0.1);
    border: 1px solid rgba(96, 165, 250, 0.2);
    border-radius: 50px;
    color: #60a5fa;
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 1.5rem;
  }

  .services-title {
    font-family: 'Inter Tight', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    line-height: 1.2;
    color: #e5e5e5;
    margin-bottom: 1.5rem;
  }

  .gradient-text {
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .services-description {
    font-size: 1.125rem;
    color: #a3a3a3;
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
    margin-bottom: 5rem;
  }

  .service-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 24px;
    padding: 2rem;
    backdrop-filter: blur(8px);
    transition: all 400ms cubic-bezier(0.22, 0.61, 0.36, 1);
    position: relative;
    overflow: hidden;
  }

  .service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(96, 165, 250, 0.3), transparent);
    opacity: 0;
    transition: opacity 300ms ease;
  }

  .service-card:hover {
    transform: translateY(-8px);
    border-color: rgba(96, 165, 250, 0.2);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
  }

  .service-card:hover::before {
    opacity: 1;
  }

  .service-card.featured {
    border-color: rgba(96, 165, 250, 0.3);
    background: rgba(96, 165, 250, 0.05);
  }

  .card-header {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-bottom: 1.5rem;
    position: relative;
  }

  .service-icon {
    width: fit-content;
  }

  .icon-background {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    box-shadow: 0 8px 25px rgba(96, 165, 250, 0.25);
  }

  .card-header h3 {
    font-family: 'Inter Tight', sans-serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: #e5e5e5;
    line-height: 1.3;
  }

  .featured-badge {
    position: absolute;
    top: -1rem;
    right: -1rem;
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 0 24px 0 16px;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .card-content {
    margin-bottom: 2rem;
  }

  .card-content p {
    color: #d4d4d4;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .service-features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .service-features li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: #d4d4d4;
    font-size: 0.95rem;
  }

  .feature-icon {
    width: 20px;
    height: 20px;
    background: rgba(34, 197, 94, 0.2);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #22c55e;
    font-size: 0.75rem;
    font-weight: bold;
    flex-shrink: 0;
  }

  .card-footer {
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    padding-top: 1.5rem;
  }

  .service-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: #60a5fa;
    text-decoration: none;
    font-weight: 500;
    transition: all 200ms ease;
  }

  .service-cta:hover {
    color: #93c5fd;
    transform: translateX(4px);
  }

  .service-cta svg {
    transition: transform 200ms ease;
  }

  .service-cta:hover svg {
    transform: translateX(2px);
  }

  /* Sección de departamentos */
  .departments-section {
    text-align: center;
    padding-top: 3rem;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .departments-title {
    font-family: 'Inter Tight', sans-serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: #e5e5e5;
    margin-bottom: 2rem;
  }

  .departments-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .department-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    padding: 1.5rem 1rem;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    transition: all 300ms ease;
    backdrop-filter: blur(8px);
  }

  .department-item:hover {
    transform: translateY(-4px);
    border-color: rgba(96, 165, 250, 0.2);
    background: rgba(96, 165, 250, 0.05);
  }

  .dept-icon {
    width: 50px;
    height: 50px;
    background: rgba(96, 165, 250, 0.1);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
  }

  .department-item span {
    color: #d4d4d4;
    font-weight: 500;
    font-size: 0.95rem;
    text-align: center;
  }

  /* Animaciones de entrada */
  .service-card {
    opacity: 0;
    transform: translateY(30px);
    animation: card-reveal 600ms ease forwards;
  }

  .service-card:nth-child(1) { animation-delay: 0ms; }
  .service-card:nth-child(2) { animation-delay: 200ms; }
  .service-card:nth-child(3) { animation-delay: 400ms; }

  @keyframes card-reveal {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .services-ia-section {
      padding: 4rem 0;
    }

    .container {
      padding: 0 1rem;
    }

    .services-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .service-card {
      padding: 1.5rem;
    }

    .departments-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }

    .department-item {
      padding: 1rem;
    }

    .dept-icon {
      width: 40px;
      height: 40px;
      font-size: 1.25rem;
    }
  }

  @media (max-width: 480px) {
    .departments-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Intersection Observer para animaciones on scroll
    const observerOptions = {
      threshold: 0.2,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('animate-in');
        }
      });
    }, observerOptions);

    // Observar cards de servicios
    const serviceCards = document.querySelectorAll('.service-card');
    serviceCards.forEach(card => observer.observe(card));

    // Observar departamentos
    const departmentItems = document.querySelectorAll('.department-item');
    departmentItems.forEach((item, index) => {
      item.style.animationDelay = `${index * 100}ms`;
      observer.observe(item);
    });

    // Efecto hover con Lottie-like animation
    serviceCards.forEach(card => {
      card.addEventListener('mouseenter', () => {
        const icon = card.querySelector('.icon-background');
        if (icon) {
          icon.style.transform = 'scale(1.1) rotate(5deg)';
        }
      });

      card.addEventListener('mouseleave', () => {
        const icon = card.querySelector('.icon-background');
        if (icon) {
          icon.style.transform = 'scale(1) rotate(0deg)';
        }
      });
    });
  });
</script>
