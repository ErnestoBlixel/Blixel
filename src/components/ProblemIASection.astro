---
// Sección del Problema - El 42% del tiempo operativo se pierde en tareas que la IA puede automatizar
---

<section class="problem-ia-section">
  <div class="container">
    <div class="problem-content">
      <div class="problem-text">
        <h2 class="problem-title">
          La inteligencia artificial no es el futuro de tu empresa.
          <span class="gradient-text">Es su presente.</span>
        </h2>
        
        <p class="problem-description">
          Cada día que pasa sin implementarla, tu competencia automatiza procesos, mejora decisiones y transforma la experiencia de sus clientes.
        </p>

        <div class="cta-message">
          <p class="cta-text">El momento de actuar es ahora</p>
          <p class="cta-subtitle">TRANSFORMACIÓN DIGITAL CON IA</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .problem-ia-section {
    padding: 3rem 0; /* Reducido de 6rem */
    background: transparent; /* Transparente para ver particles */
    position: relative;
    overflow: hidden;
  }

  .problem-ia-section::before {
    display: none; /* Eliminar línea decorativa */
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .problem-content {
    text-align: center;
    max-width: 900px;
    margin: 0 auto;
  }

  /* Contenido del texto */
  .problem-text {
    display: flex;
    flex-direction: column;
    gap: 1.5rem; /* Reducido de 2rem */
    align-items: center;
  }

  .problem-title {
    font-family: 'Inter', -apple-system, sans-serif;
    font-size: clamp(1.75rem, 3.5vw, 2.5rem); /* Reducido significativamente */
    font-weight: 700;
    line-height: 1.3;
    color: #e5e5e5;
    margin-bottom: 1rem;
  }

  .gradient-text {
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    display: block; /* Para que quede en línea separada */
  }

  .problem-description {
    font-size: 1rem; /* Reducido de 1.125rem */
    color: #a3a3a3;
    line-height: 1.6;
    max-width: 700px;
    margin: 0 auto;
  }

  .cta-message {
    margin-top: 2rem;
  }

  .cta-text {
    font-size: 1.125rem;
    color: #02aa6d;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }

  .cta-subtitle {
    font-size: 0.875rem;
    color: #9ca3af;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* Animaciones simplificadas */
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .problem-title,
  .problem-description,
  .cta-message {
    animation: fade-in 0.8s ease-out;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .problem-ia-section {
      padding: 2rem 0; /* Aún menos padding en móvil */
    }

    .container {
      padding: 0 1rem;
    }

    .problem-title {
      font-size: 1.5rem;
    }

    .problem-description {
      font-size: 0.9rem;
    }

    .cta-text {
      font-size: 1rem;
    }

    .cta-subtitle {
      font-size: 0.75rem;
    }
  }
</style>

<script>
  // Script simplificado - solo animación de entrada
  document.addEventListener('DOMContentLoaded', function() {
    const observerOptions = {
      threshold: 0.3,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, observerOptions);

    const section = document.querySelector('.problem-ia-section');
    if (section) {
      observer.observe(section);
    }
  });
</script>
