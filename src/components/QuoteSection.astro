---
// Sección: Cita inspiradora sobre transformación digital con IA
---

<section class="quote-section">
  <!-- Fondo con efecto de gradiente animado -->
  <div class="quote__background">
    <div class="gradient-pulse"></div>
  </div>

  <div class="container">
    <div class="quote-wrapper">
      <!-- Icono de comillas -->
      <div class="quote-icon">
        <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M14 28C14 28 10 24 10 16C10 8 14 4 14 4L18 8C18 8 16 10 16 16C16 18 16 20 16 20H20C21.1046 20 22 20.8954 22 22V36C22 37.1046 21.1046 38 20 38H10C8.89543 38 8 37.1046 8 36V28C8 26.8954 8.89543 26 10 26H14V28Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          <path d="M36 28C36 28 32 24 32 16C32 8 36 4 36 4L40 8C40 8 38 10 38 16C38 18 38 20 38 20H42C43.1046 20 44 20.8954 44 22V36C44 37.1046 43.1046 38 42 38H32C30.8954 38 30 37.1046 30 36V28C30 26.8954 30.8954 26 32 26H36V28Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>

      <!-- Contenido de la cita -->
      <blockquote class="quote-content">
        <p class="quote-text">
          La inteligencia artificial no es el futuro de tu empresa, <span class="highlight">es el presente</span>. 
          Cada día que pasa sin implementarla, tu competencia automatiza procesos, mejora decisiones y 
          transforma la experiencia de sus clientes.
        </p>
        <footer class="quote-footer">
          <cite class="quote-author">El momento de actuar es ahora</cite>
          <span class="quote-context">Transformación digital con IA</span>
        </footer>
      </blockquote>

      <!-- Estadísticas flotantes -->
      <div class="stats-container">
        <div class="stat-item stat-left">
          <span class="stat-number">67%</span>
          <span class="stat-label">de empresas con IA reportan mejoras significativas</span>
        </div>
        <div class="stat-item stat-right">
          <span class="stat-number">46%</span>
          <span class="stat-label">de trabajos administrativos pueden automatizarse hoy</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .quote-section {
    position: relative;
    background: #000000;
    padding: 8rem 0;
    overflow: hidden;
    border: none;
    box-shadow: none;
  }

  /* Fondo animado */
  .quote__background {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .gradient-pulse {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 800px;
    height: 400px;
    background: radial-gradient(ellipse at center, rgba(37, 24, 99, 0.2) 0%, transparent 70%);
    filter: blur(60px);
    animation: pulse-glow 4s ease-in-out infinite;
  }

  @keyframes pulse-glow {
    0%, 100% { 
      opacity: 0.3;
      transform: translate(-50%, -50%) scale(1);
    }
    50% { 
      opacity: 0.6;
      transform: translate(-50%, -50%) scale(1.1);
    }
  }

  .container {
    position: relative;
    z-index: 10;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .quote-wrapper {
    position: relative;
    text-align: center;
    max-width: 900px;
    margin: 0 auto;
  }

  /* Icono de comillas */
  .quote-icon {
    display: inline-flex;
    margin-bottom: 2rem;
    opacity: 0;
    transform: translateY(20px);
    animation: fade-in-up 800ms ease forwards;
  }

  .quote-icon svg {
    width: 60px;
    height: 60px;
    color: #251863;
    opacity: 0.5;
  }

  /* Contenido de la cita */
  .quote-content {
    margin: 0;
    opacity: 0;
    transform: translateY(30px);
    animation: fade-in-up 800ms ease forwards;
    animation-delay: 200ms;
  }

  .quote-text {
    font-family: 'Inter', -apple-system, sans-serif;
    font-size: clamp(1.5rem, 3vw, 2.25rem);
    font-weight: 300;
    line-height: 1.6;
    color: #e5e5e5;
    margin-bottom: 2rem;
    letter-spacing: -0.01em;
  }

  .highlight {
    font-weight: 600;
    background: linear-gradient(135deg, #251863 0%, #02aa6d 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .quote-footer {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .quote-author {
    font-size: 1.125rem;
    font-weight: 500;
    color: #02aa6d;
    font-style: normal;
  }

  .quote-context {
    font-size: 0.875rem;
    color: #6b7280;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* Estadísticas flotantes */
  .stats-container {
    position: absolute;
    top: 50%;
    left: 0;
    right: 0;
    transform: translateY(-50%);
    pointer-events: none;
  }

  .stat-item {
    position: absolute;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 16px;
    padding: 1.5rem 2rem;
    backdrop-filter: blur(10px);
    opacity: 0;
    animation: float-in 1s ease forwards;
  }

  .stat-left {
    left: -80px;
    top: -60px;
    animation-delay: 600ms;
  }

  .stat-right {
    right: -80px;
    bottom: -60px;
    animation-delay: 800ms;
  }

  .stat-number {
    display: block;
    font-size: 2.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #251863 0%, #02aa6d 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.5rem;
  }

  .stat-label {
    display: block;
    font-size: 0.875rem;
    color: #9ca3af;
    max-width: 180px;
    line-height: 1.4;
  }

  @keyframes fade-in-up {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes float-in {
    to {
      opacity: 0.9;
    }
  }

  /* Efecto hover en las estadísticas */
  @media (hover: hover) {
    .stat-item {
      transition: transform 300ms ease;
    }
    
    .quote-section:hover .stat-left {
      transform: translateX(10px) translateY(5px);
    }
    
    .quote-section:hover .stat-right {
      transform: translateX(-10px) translateY(-5px);
    }
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .stat-item {
      display: none; /* Ocultar estadísticas flotantes en tablets */
    }
    
    .quote-section {
      padding: 6rem 0;
    }
  }

  @media (max-width: 768px) {
    .quote-section {
      padding: 4rem 0;
    }

    .container {
      padding: 0 1.5rem;
    }

    .quote-icon svg {
      width: 48px;
      height: 48px;
    }

    .quote-text {
      font-size: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .gradient-pulse {
      width: 400px;
      height: 200px;
    }
  }

  @media (max-width: 480px) {
    .quote-section {
      padding: 3rem 0;
    }

    .container {
      padding: 0 1rem;
    }

    .quote-text {
      font-size: 1.25rem;
      line-height: 1.5;
    }

    .quote-author {
      font-size: 1rem;
    }

    .quote-context {
      font-size: 0.75rem;
    }
  }
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Intersection Observer para animación cuando entra en viewport
  const observerOptions = {
    threshold: 0.3,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('in-view');
        
        // Animar las estadísticas con un pequeño retraso
        const stats = entry.target.querySelectorAll('.stat-item');
        stats.forEach((stat, index) => {
          setTimeout(() => {
            stat.style.opacity = '0.9';
          }, 600 + (index * 200));
        });
      }
    });
  }, observerOptions);

  const quoteSection = document.querySelector('.quote-section');
  if (quoteSection) {
    observer.observe(quoteSection);
  }

  // Efecto parallax suave en el fondo
  let ticking = false;
  function updateParallax() {
    if (!ticking) {
      window.requestAnimationFrame(() => {
        const scrolled = window.pageYOffset;
        const gradientPulse = document.querySelector('.gradient-pulse');
        
        if (gradientPulse) {
          const speed = 0.5;
          const yPos = -(scrolled * speed);
          gradientPulse.style.transform = `translate(-50%, calc(-50% + ${yPos}px)) scale(1)`;
        }
        
        ticking = false;
      });
      
      ticking = true;
    }
  }

  window.addEventListener('scroll', updateParallax);
});
</script>
