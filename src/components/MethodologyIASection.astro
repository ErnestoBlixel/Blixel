---
// Sección: Metodología Quick-ROI: IA en 90 días
---

<section id="metodo" class="methodology-section">
  <div class="container">
    <div class="methodology-header">
      <div class="section-badge">
        <span>Nuestra Metodología</span>
      </div>
      
      <h2 class="methodology-title">
        Metodología <span class="gradient-text">Quick-ROI</span>: IA en 90 días
      </h2>
      
      <p class="methodology-description">
        Nuestro proceso probado garantiza resultados medibles en menos de 3 meses, minimizando riesgos y maximizando el retorno de inversión.
      </p>
    </div>

    <div class="timeline-container">
      <div class="timeline-line"></div>
      
      <!-- Fase 1: Auditoría -->
      <div class="timeline-item" data-phase="1">
        <div class="timeline-marker">
          <div class="marker-number">1</div>
        </div>
        <div class="timeline-content">
          <div class="phase-header">
            <h3>Auditoría</h3>
            <span class="phase-duration">Días 1-15</span>
          </div>
          <p class="phase-description">
            Analizamos tus procesos actuales, identificamos oportunidades de automatización y definimos el alcance del proyecto de IA.
          </p>
          <ul class="phase-deliverables">
            <li>
              <div class="deliverable-icon">📋</div>
              <span>Mapeo de procesos empresariales</span>
            </li>
            <li>
              <div class="deliverable-icon">🎯</div>
              <span>Identificación de casos de uso prioritarios</span>
            </li>
            <li>
              <div class="deliverable-icon">📊</div>
              <span>Análisis de ROI potencial</span>
            </li>
            <li>
              <div class="deliverable-icon">📈</div>
              <span>Roadmap de implementación</span>
            </li>
          </ul>
        </div>
      </div>

      <!-- Fase 2: Piloto -->
      <div class="timeline-item" data-phase="2">
        <div class="timeline-marker">
          <div class="marker-number">2</div>
        </div>
        <div class="timeline-content">
          <div class="phase-header">
            <h3>Piloto</h3>
            <span class="phase-duration">Días 16-45</span>
          </div>
          <p class="phase-description">
            Desarrollamos y desplegamos un piloto funcional en un área específica de tu empresa para validar el concepto y ajustar la solución.
          </p>
          <ul class="phase-deliverables">
            <li>
              <div class="deliverable-icon">🛠️</div>
              <span>Desarrollo del MVP de IA</span>
            </li>
            <li>
              <div class="deliverable-icon">🔧</div>
              <span>Integración con sistemas existentes</span>
            </li>
            <li>
              <div class="deliverable-icon">👥</div>
              <span>Formación del equipo piloto</span>
            </li>
            <li>
              <div class="deliverable-icon">🧪</div>
              <span>Testing y optimización inicial</span>
            </li>
          </ul>
        </div>
      </div>

      <!-- Fase 3: Métricas -->
      <div class="timeline-item" data-phase="3">
        <div class="timeline-marker">
          <div class="marker-number">3</div>
        </div>
        <div class="timeline-content">
          <div class="phase-header">
            <h3>Métricas</h3>
            <span class="phase-duration">Días 46-75</span>
          </div>
          <p class="phase-description">
            Medimos el impacto real, recopilamos datos de rendimiento y validamos el ROI obtenido con la implementación del piloto.
          </p>
          <ul class="phase-deliverables">
            <li>
              <div class="deliverable-icon">📈</div>
              <span>Dashboard de métricas en tiempo real</span>
            </li>
            <li>
              <div class="deliverable-icon">💰</div>
              <span>Cálculo de ROI real</span>
            </li>
            <li>
              <div class="deliverable-icon">📋</div>
              <span>Informe de resultados detallado</span>
            </li>
            <li>
              <div class="deliverable-icon">🔍</div>
              <span>Análisis de mejoras y optimizaciones</span>
            </li>
          </ul>
        </div>
      </div>

      <!-- Fase 4: Escalado -->
      <div class="timeline-item" data-phase="4">
        <div class="timeline-marker">
          <div class="marker-number">4</div>
        </div>
        <div class="timeline-content">
          <div class="phase-header">
            <h3>Escalado</h3>
            <span class="phase-duration">Días 76-90</span>
          </div>
          <p class="phase-description">
            Expandimos la solución a otros departamentos o procesos, asegurando una adopción exitosa en toda la organización.
          </p>
          <ul class="phase-deliverables">
            <li>
              <div class="deliverable-icon">🚀</div>
              <span>Plan de escalado empresarial</span>
            </li>
            <li>
              <div class="deliverable-icon">👨‍🏫</div>
              <span>Formación masiva de empleados</span>
            </li>
            <li>
              <div class="deliverable-icon">🛡️</div>
              <span>Sistemas de monitoreo y mantenimiento</span>
            </li>
            <li>
              <div class="deliverable-icon">📞</div>
              <span>Soporte técnico continuado</span>
            </li>
          </ul>
        </div>
      </div>
    </div>

    <!-- Garantía de resultados -->
    <div class="guarantee-section">
      <div class="guarantee-card">
        <div class="guarantee-icon">🎯</div>
        <div class="guarantee-content">
          <h3>Garantía de Resultados</h3>
          <p>Si no obtienes un ROI positivo en 90 días, continuamos trabajando sin costo adicional hasta conseguirlo.</p>
        </div>
        <div class="guarantee-badge">
          <span>100% Garantizado</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .methodology-section {
    padding: 6rem 0;
    background: #0d0d0d;
    position: relative;
    overflow: hidden;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .methodology-header {
    text-align: center;
    margin-bottom: 5rem;
  }

  .section-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(96, 165, 250, 0.1);
    border: 1px solid rgba(96, 165, 250, 0.2);
    border-radius: 50px;
    color: #60a5fa;
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 1.5rem;
  }

  .methodology-title {
    font-family: 'Inter Tight', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    line-height: 1.2;
    color: #e5e5e5;
    margin-bottom: 1.5rem;
  }

  .gradient-text {
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .methodology-description {
    font-size: 1.125rem;
    color: #a3a3a3;
    max-width: 700px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .timeline-container {
    position: relative;
    max-width: 1000px;
    margin: 0 auto 5rem;
  }

  .timeline-line {
    position: absolute;
    left: 50%;
    top: 0;
    bottom: 0;
    width: 2px;
    background: linear-gradient(to bottom, #60a5fa, #a855f7);
    transform: translateX(-50%);
    opacity: 0.3;
  }

  .timeline-item {
    display: flex;
    align-items: flex-start;
    margin-bottom: 4rem;
    opacity: 0;
    transform: translateY(30px);
    animation: timeline-reveal 600ms ease forwards;
  }

  .timeline-item:nth-child(even) {
    flex-direction: row-reverse;
  }

  .timeline-item:nth-child(1) { animation-delay: 0ms; }
  .timeline-item:nth-child(2) { animation-delay: 200ms; }
  .timeline-item:nth-child(3) { animation-delay: 400ms; }
  .timeline-item:nth-child(4) { animation-delay: 600ms; }

  .timeline-marker {
    position: relative;
    z-index: 10;
    margin: 0 2rem;
    flex-shrink: 0;
  }

  .marker-number {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-family: 'Inter Tight', sans-serif;
    font-size: 1.5rem;
    font-weight: 700;
    box-shadow: 0 0 30px rgba(96, 165, 250, 0.4);
  }

  .timeline-content {
    flex: 1;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 24px;
    padding: 2rem;
    backdrop-filter: blur(8px);
    transition: all 300ms ease;
  }

  .timeline-content:hover {
    transform: translateY(-4px);
    border-color: rgba(96, 165, 250, 0.2);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
  }

  .phase-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }

  .phase-header h3 {
    font-family: 'Inter Tight', sans-serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: #e5e5e5;
  }

  .phase-duration {
    background: rgba(96, 165, 250, 0.1);
    color: #60a5fa;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.875rem;
    font-weight: 500;
  }

  .phase-description {
    color: #d4d4d4;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .phase-deliverables {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .phase-deliverables li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: #d4d4d4;
    font-size: 0.95rem;
  }

  .deliverable-icon {
    width: 32px;
    height: 32px;
    background: rgba(96, 165, 250, 0.1);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.875rem;
    flex-shrink: 0;
  }

  /* Garantía */
  .guarantee-section {
    max-width: 800px;
    margin: 0 auto;
  }

  .guarantee-card {
    background: linear-gradient(135deg, rgba(96, 165, 250, 0.1), rgba(168, 85, 247, 0.1));
    border: 1px solid rgba(96, 165, 250, 0.2);
    border-radius: 24px;
    padding: 2rem;
    display: flex;
    align-items: center;
    gap: 1.5rem;
    position: relative;
    overflow: hidden;
  }

  .guarantee-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(96, 165, 250, 0.5), transparent);
  }

  .guarantee-icon {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    flex-shrink: 0;
  }

  .guarantee-content {
    flex: 1;
  }

  .guarantee-content h3 {
    font-family: 'Inter Tight', sans-serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: #e5e5e5;
    margin-bottom: 0.5rem;
  }

  .guarantee-content p {
    color: #d4d4d4;
    line-height: 1.6;
  }

  .guarantee-badge {
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    color: white;
    padding: 0.75rem 1.5rem;
    border-radius: 12px;
    font-weight: 600;
    text-transform: uppercase;
    font-size: 0.875rem;
    letter-spacing: 0.05em;
    flex-shrink: 0;
  }

  /* Animaciones */
  @keyframes timeline-reveal {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .timeline-line {
      left: 30px;
    }

    .timeline-item {
      flex-direction: row !important;
    }

    .timeline-item:nth-child(even) {
      flex-direction: row !important;
    }

    .timeline-marker {
      margin: 0 2rem 0 0;
    }
  }

  @media (max-width: 768px) {
    .methodology-section {
      padding: 4rem 0;
    }

    .container {
      padding: 0 1rem;
    }

    .timeline-line {
      left: 20px;
    }

    .timeline-marker {
      margin: 0 1rem 0 0;
    }

    .marker-number {
      width: 40px;
      height: 40px;
      font-size: 1.25rem;
    }

    .timeline-content {
      padding: 1.5rem;
    }

    .phase-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
    }

    .guarantee-card {
      flex-direction: column;
      text-align: center;
      gap: 1rem;
    }

    .guarantee-badge {
      align-self: stretch;
      text-align: center;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Intersection Observer para animaciones
    const observerOptions = {
      threshold: 0.3,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('animate-in');
        }
      });
    }, observerOptions);

    // Observar elementos del timeline
    const timelineItems = document.querySelectorAll('.timeline-item');
    timelineItems.forEach(item => observer.observe(item));

    // Observar la garantía
    const guaranteeCard = document.querySelector('.guarantee-card');
    if (guaranteeCard) {
      observer.observe(guaranteeCard);
    }

    // Progreso de la línea del timeline
    const timelineLine = document.querySelector('.timeline-line');
    if (timelineLine) {
      const updateTimelineProgress = () => {
        const rect = timelineLine.getBoundingClientRect();
        const viewportHeight = window.innerHeight;
        const progress = Math.max(0, Math.min(1, (viewportHeight - rect.top) / rect.height));
        
        timelineLine.style.background = `linear-gradient(to bottom, 
          #60a5fa 0%, 
          #a855f7 ${progress * 100}%, 
          rgba(96, 165, 250, 0.2) ${progress * 100}%, 
          rgba(96, 165, 250, 0.2) 100%
        )`;
      };

      let ticking = false;
      const onScroll = () => {
        if (!ticking) {
          requestAnimationFrame(() => {
            updateTimelineProgress();
            ticking = false;
          });
          ticking = true;
        }
      };

      window.addEventListener('scroll', onScroll);
      updateTimelineProgress();
    }
  });
</script>
