---
// Componente para la sección de Metodología con timeline horizontal
---

<section class="methodology-section">
  <div class="container">
    <div class="section-header">
      <div class="section-badge">
        <span>Nuestra Metodología</span>
      </div>
      
      <h2 class="section-title reveal-text">
        Proceso probado para garantizar el éxito
      </h2>
      
      <p class="section-description reveal-text" data-delay="200">
        Seguimos una metodología estructurada que asegura la implementación exitosa de soluciones de IA en tu empresa.
      </p>
    </div>

    <div class="timeline-container">
      <div class="timeline-line">
        <div class="timeline-progress"></div>
      </div>
      
      <div class="timeline-steps">
        <div class="timeline-step" data-step="1">
          <div class="step-circle">
            <span class="step-number">1</span>
            <svg class="step-checkmark" width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M6 10L8.5 12.5L14 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="step-content">
            <h3 class="step-title">Análisis</h3>
            <p class="step-description">
              Evaluamos tus procesos actuales y identificamos oportunidades de automatización.
            </p>
            <div class="step-duration">2-3 semanas</div>
          </div>
        </div>

        <div class="timeline-step" data-step="2">
          <div class="step-circle">
            <span class="step-number">2</span>
            <svg class="step-checkmark" width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M6 10L8.5 12.5L14 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="step-content">
            <h3 class="step-title">Diseño</h3>
            <p class="step-description">
              Creamos una arquitectura personalizada que se adapta a tus necesidades específicas.
            </p>
            <div class="step-duration">1-2 semanas</div>
          </div>
        </div>

        <div class="timeline-step" data-step="3">
          <div class="step-circle">
            <span class="step-number">3</span>
            <svg class="step-checkmark" width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M6 10L8.5 12.5L14 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="step-content">
            <h3 class="step-title">Desarrollo</h3>
            <p class="step-description">
              Implementamos la solución con tecnologías de vanguardia y las mejores prácticas.
            </p>
            <div class="step-duration">3-6 semanas</div>
          </div>
        </div>

        <div class="timeline-step" data-step="4">
          <div class="step-circle">
            <span class="step-number">4</span>
            <svg class="step-checkmark" width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M6 10L8.5 12.5L14 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="step-content">
            <h3 class="step-title">Testing</h3>
            <p class="step-description">
              Realizamos pruebas exhaustivas para garantizar el funcionamiento óptimo.
            </p>
            <div class="step-duration">1 semana</div>
          </div>
        </div>

        <div class="timeline-step" data-step="5">
          <div class="step-circle">
            <span class="step-number">5</span>
            <svg class="step-checkmark" width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M6 10L8.5 12.5L14 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="step-content">
            <h3 class="step-title">Lanzamiento</h3>
            <p class="step-description">
              Desplegamos la solución y brindamos capacitación a tu equipo.
            </p>
            <div class="step-duration">1 semana</div>
          </div>
        </div>

        <div class="timeline-step" data-step="6">
          <div class="step-circle">
            <span class="step-number">6</span>
            <svg class="step-checkmark" width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M6 10L8.5 12.5L14 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="step-content">
            <h3 class="step-title">Soporte</h3>
            <p class="step-description">
              Ofrecemos soporte continuo y optimizaciones para maximizar los resultados.
            </p>
            <div class="step-duration">Continuo</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .methodology-section {
    padding: 8rem 0;
    background: #0d0d0d;
    position: relative;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .section-header {
    text-align: center;
    margin-bottom: 6rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .section-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(96, 165, 250, 0.1);
    border: 1px solid rgba(96, 165, 250, 0.2);
    border-radius: 50px;
    color: #60a5fa;
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 2rem;
  }

  .section-title {
    font-family: 'Inter Tight', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 600;
    line-height: 1.2;
    color: #e5e5e5;
    margin-bottom: 1.5rem;
    letter-spacing: -0.01em;
  }

  .section-description {
    font-size: 1.25rem;
    line-height: 1.6;
    color: #a3a3a3;
    margin: 0;
  }

  .timeline-container {
    position: relative;
    max-width: 1200px;
    margin: 0 auto;
  }

  .timeline-line {
    position: absolute;
    top: 48px;
    left: 0;
    right: 0;
    height: 1px;
    background: #2e2e2e;
    z-index: 1;
  }

  .timeline-progress {
    height: 100%;
    background: linear-gradient(90deg, #60a5fa, #60a5fa);
    width: 0%;
    transition: width 2s ease;
  }

  .timeline-steps {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 2rem;
    position: relative;
    z-index: 2;
  }

  .timeline-step {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    opacity: 0;
    transform: translateY(20px);
    transition: all 600ms ease;
  }

  .timeline-step.animated {
    opacity: 1;
    transform: translateY(0);
  }

  .step-circle {
    width: 48px;
    height: 48px;
    background: #171717;
    border: 2px solid #2e2e2e;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    position: relative;
    transition: all 600ms ease;
  }

  .step-circle.active {
    border-color: #60a5fa;
    background: rgba(96, 165, 250, 0.1);
    box-shadow: 0 0 20px rgba(96, 165, 250, 0.3);
  }

  .step-circle.completed {
    background: #60a5fa;
    border-color: #60a5fa;
  }

  .step-number {
    font-size: 1rem;
    font-weight: 600;
    color: #737373;
    transition: color 600ms ease;
  }

  .step-circle.active .step-number {
    color: #60a5fa;
  }

  .step-circle.completed .step-number {
    opacity: 0;
  }

  .step-checkmark {
    position: absolute;
    color: white;
    opacity: 0;
    transform: scale(0);
    transition: all 600ms ease;
  }

  .step-circle.completed .step-checkmark {
    opacity: 1;
    transform: scale(1);
  }

  .step-content {
    max-width: 200px;
  }

  .step-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: #e5e5e5;
    margin-bottom: 0.75rem;
    line-height: 1.3;
  }

  .step-description {
    color: #a3a3a3;
    font-size: 0.875rem;
    line-height: 1.5;
    margin-bottom: 1rem;
  }

  .step-duration {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background: rgba(96, 165, 250, 0.1);
    border: 1px solid rgba(96, 165, 250, 0.2);
    border-radius: 20px;
    color: #60a5fa;
    font-size: 0.75rem;
    font-weight: 500;
  }

  .reveal-text {
    opacity: 0;
    transform: translateY(15px);
    transition: all 200ms ease;
  }

  .reveal-text.revealed {
    opacity: 1;
    transform: translateY(0);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .timeline-steps {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 768px) {
    .methodology-section {
      padding: 4rem 0;
    }

    .container {
      padding: 0 1rem;
    }

    .section-header {
      margin-bottom: 4rem;
    }

    .timeline-steps {
      grid-template-columns: repeat(2, 1fr);
      gap: 3rem 1rem;
    }

    .timeline-line {
      display: none;
    }

    .step-content {
      max-width: 160px;
    }
  }

  @media (max-width: 480px) {
    .timeline-steps {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .step-content {
      max-width: 250px;
    }
  }
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Intersection Observer para reveal animations
  const revealElements = document.querySelectorAll('.reveal-text');
  
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const delay = parseInt(entry.target.dataset.delay) || 0;
        setTimeout(() => {
          entry.target.classList.add('revealed');
        }, delay);
        revealObserver.unobserve(entry.target);
      }
    });
  }, {
    threshold: 0.1
  });
  
  revealElements.forEach(el => revealObserver.observe(el));

  // Animación del timeline
  const timelineSteps = document.querySelectorAll('.timeline-step');
  const timelineProgress = document.querySelector('.timeline-progress');
  
  const timelineObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        animateTimeline();
        timelineObserver.unobserve(entry.target);
      }
    });
  }, {
    threshold: 0.3
  });
  
  if (timelineSteps.length > 0) {
    timelineObserver.observe(timelineSteps[0]);
  }
  
  function animateTimeline() {
    // Animar pasos del timeline
    timelineSteps.forEach((step, index) => {
      setTimeout(() => {
        step.classList.add('animated');
        
        const circle = step.querySelector('.step-circle');
        
        // Activar círculo
        setTimeout(() => {
          circle.classList.add('active');
        }, 200);
        
        // Completar círculo después de un tiempo
        setTimeout(() => {
          circle.classList.add('completed');
        }, 800);
        
      }, index * 200);
    });
    
    // Animar línea de progreso
    setTimeout(() => {
      timelineProgress.style.width = '100%';
    }, 500);
  }

  // Scroll horizontal en móvil para el timeline
  if (window.innerWidth <= 768) {
    const timelineContainer = document.querySelector('.timeline-container');
    let isScrolling = false;
    
    timelineContainer.addEventListener('wheel', (e) => {
      if (Math.abs(e.deltaY) > Math.abs(e.deltaX)) {
        e.preventDefault();
        
        if (!isScrolling) {
          isScrolling = true;
          timelineContainer.scrollLeft += e.deltaY;
          
          setTimeout(() => {
            isScrolling = false;
          }, 50);
        }
      }
    }, { passive: false });
  }
});
</script>
