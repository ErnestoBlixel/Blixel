---
// Sección: Casos de Inteligencia Artificial en Empresas reales
---

<section id="casos" class="cases-section">
  <div class="container">
    <div class="cases-header">
      <div class="section-badge">
        <span>Casos de Éxito</span>
      </div>
      
      <h2 class="cases-title">
        Casos de <span class="gradient-text">Inteligencia Artificial en Empresas</span> reales
      </h2>
      
      <p class="cases-description">
        Descubre cómo hemos transformado empresas de diferentes sectores con soluciones de IA personalizadas.
      </p>
    </div>

    <div class="cases-slider">
      <div class="cases-track" id="casesTrack">
        <!-- Caso 1: Marketing -->
        <div class="case-card" data-sector="marketing">
          <div class="case-header">
            <div class="sector-icon">💼</div>
            <div class="sector-info">
              <h3>Marketing Digital</h3>
              <span class="company-type">Agencia de Marketing</span>
            </div>
            <div class="result-badge">
              <span>+270% conversión</span>
            </div>
          </div>
          
          <div class="case-content">
            <p class="case-description">
              Implementamos un sistema de IA que automatiza la segmentación de audiencias y personaliza campañas en tiempo real.
            </p>
            
            <div class="case-results">
              <div class="result-item">
                <div class="result-number">270%</div>
                <div class="result-label">Aumento en conversiones</div>
              </div>
              <div class="result-item">
                <div class="result-number">60%</div>
                <div class="result-label">Reducción en CAC</div>
              </div>
              <div class="result-item">
                <div class="result-number">40h</div>
                <div class="result-label">Ahorro semanal</div>
              </div>
            </div>
            
            <div class="case-features">
              <div class="feature-tag">Segmentación automática</div>
              <div class="feature-tag">Personalización en tiempo real</div>
              <div class="feature-tag">Análisis predictivo</div>
            </div>
          </div>
          
          <div class="case-overlay">
            <span>Ejemplo de IA en empresas de Marketing</span>
          </div>
        </div>

        <!-- Caso 2: Finanzas -->
        <div class="case-card" data-sector="finanzas">
          <div class="case-header">
            <div class="sector-icon">💰</div>
            <div class="sector-info">
              <h3>Finanzas</h3>
              <span class="company-type">Empresa de Servicios Financieros</span>
            </div>
            <div class="result-badge">
              <span>+85% precisión</span>
            </div>
          </div>
          
          <div class="case-content">
            <p class="case-description">
              Desarrollamos un modelo de IA para análisis de riesgo crediticio que automatiza la evaluación de préstamos empresariales.
            </p>
            
            <div class="case-results">
              <div class="result-item">
                <div class="result-number">85%</div>
                <div class="result-label">Precisión en análisis</div>
              </div>
              <div class="result-item">
                <div class="result-number">90%</div>
                <div class="result-label">Reducción de tiempo</div>
              </div>
              <div class="result-item">
                <div class="result-number">€2M</div>
                <div class="result-label">Ahorro anual</div>
              </div>
            </div>
            
            <div class="case-features">
              <div class="feature-tag">Análisis de riesgo automatizado</div>
              <div class="feature-tag">Detección de fraudes</div>
              <div class="feature-tag">Reportes inteligentes</div>
            </div>
          </div>
          
          <div class="case-overlay">
            <span>Ejemplo de IA en empresas de Finanzas</span>
          </div>
        </div>

        <!-- Caso 3: RRHH -->
        <div class="case-card" data-sector="rrhh">
          <div class="case-header">
            <div class="sector-icon">👥</div>
            <div class="sector-info">
              <h3>Recursos Humanos</h3>
              <span class="company-type">Multinacional Tecnológica</span>
            </div>
            <div class="result-badge">
              <span>+65% eficiencia</span>
            </div>
          </div>
          
          <div class="case-content">
            <p class="case-description">
              Creamos un asistente virtual de RRHH que automatiza procesos de selección, onboarding y gestión de empleados.
            </p>
            
            <div class="case-results">
              <div class="result-item">
                <div class="result-number">65%</div>
                <div class="result-label">Mejora en eficiencia</div>
              </div>
              <div class="result-item">
                <div class="result-number">80%</div>
                <div class="result-label">Reducción en screening</div>
              </div>
              <div class="result-item">
                <div class="result-number">95%</div>
                <div class="result-label">Satisfacción empleados</div>
              </div>
            </div>
            
            <div class="case-features">
              <div class="feature-tag">Screening automatizado</div>
              <div class="feature-tag">Chatbot de consultas</div>
              <div class="feature-tag">Análisis de sentimiento</div>
            </div>
          </div>
          
          <div class="case-overlay">
            <span>Ejemplo de IA en empresas de RRHH</span>
          </div>
        </div>

        <!-- Caso 4: Operaciones -->
        <div class="case-card" data-sector="operaciones">
          <div class="case-header">
            <div class="sector-icon">📊</div>
            <div class="sector-info">
              <h3>Operaciones</h3>
              <span class="company-type">Empresa de Logística</span>
            </div>
            <div class="result-badge">
              <span>+45% productividad</span>
            </div>
          </div>
          
          <div class="case-content">
            <p class="case-description">
              Implementamos IA predictiva para optimización de rutas, gestión de inventarios y mantenimiento preventivo de flotas.
            </p>
            
            <div class="case-results">
              <div class="result-item">
                <div class="result-number">45%</div>
                <div class="result-label">Aumento productividad</div>
              </div>
              <div class="result-item">
                <div class="result-number">30%</div>
                <div class="result-label">Reducción costos</div>
              </div>
              <div class="result-item">
                <div class="result-number">99.5%</div>
                <div class="result-label">Uptime de flota</div>
              </div>
            </div>
            
            <div class="case-features">
              <div class="feature-tag">Optimización de rutas</div>
              <div class="feature-tag">Mantenimiento predictivo</div>
              <div class="feature-tag">Gestión inteligente inventario</div>
            </div>
          </div>
          
          <div class="case-overlay">
            <span>Ejemplo de IA en empresas de logística</span>
          </div>
        </div>
      </div>

      <!-- Controles del slider -->
      <div class="slider-controls">
        <button class="slider-btn prev" id="prevBtn">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
            <path d="M12.5 15L7.5 10L12.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
        <div class="slider-dots" id="sliderDots"></div>
        <button class="slider-btn next" id="nextBtn">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
            <path d="M7.5 5L12.5 10L7.5 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
      </div>
    </div>
  </div>
</section>

<style>
  .cases-section {
    padding: 6rem 0;
    background: #171717;
    position: relative;
    overflow: hidden;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .cases-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(96, 165, 250, 0.1);
    border: 1px solid rgba(96, 165, 250, 0.2);
    border-radius: 50px;
    color: #60a5fa;
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 1.5rem;
  }

  .cases-title {
    font-family: 'Inter Tight', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    line-height: 1.2;
    color: #e5e5e5;
    margin-bottom: 1.5rem;
  }

  .gradient-text {
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .cases-description {
    font-size: 1.125rem;
    color: #a3a3a3;
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .cases-slider {
    position: relative;
    overflow: hidden;
    border-radius: 24px;
  }

  .cases-track {
    display: flex;
    transition: transform 600ms cubic-bezier(0.22, 0.61, 0.36, 1);
  }

  .case-card {
    min-width: 100%;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 24px;
    padding: 2rem;
    backdrop-filter: blur(8px);
    position: relative;
    overflow: hidden;
    transition: all 400ms ease;
  }

  .case-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(96, 165, 250, 0.3), transparent);
  }

  .case-card:hover {
    transform: perspective(1000px) rotateX(2deg) rotateY(-2deg);
    border-color: rgba(96, 165, 250, 0.2);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
  }

  .case-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 2rem;
    position: relative;
  }

  .sector-icon {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .sector-info {
    flex: 1;
  }

  .sector-info h3 {
    font-family: 'Inter Tight', sans-serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: #e5e5e5;
    margin-bottom: 0.25rem;
  }

  .company-type {
    color: #a3a3a3;
    font-size: 0.95rem;
  }

  .result-badge {
    background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 12px;
    font-weight: 600;
    font-size: 0.875rem;
    white-space: nowrap;
  }

  .case-content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .case-description {
    color: #d4d4d4;
    line-height: 1.6;
    font-size: 1.125rem;
  }

  .case-results {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .result-item {
    text-align: center;
    padding: 1rem;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
  }

  .result-number {
    font-family: 'Inter Tight', sans-serif;
    font-size: 1.75rem;
    font-weight: 700;
    color: #60a5fa;
    margin-bottom: 0.25rem;
  }

  .result-label {
    font-size: 0.875rem;
    color: #a3a3a3;
    line-height: 1.4;
  }

  .case-features {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .feature-tag {
    background: rgba(96, 165, 250, 0.1);
    color: #60a5fa;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.875rem;
    font-weight: 500;
    border: 1px solid rgba(96, 165, 250, 0.2);
  }

  .case-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    background: linear-gradient(transparent, rgba(0, 0, 0, 0.8));
    padding: 2rem 2rem 1rem;
    transform: translateY(100%);
    transition: transform 400ms ease;
  }

  .case-overlay span {
    color: #60a5fa;
    font-weight: 500;
    font-size: 0.95rem;
  }

  .case-card:hover .case-overlay {
    transform: translateY(0);
  }

  /* Controles del slider */
  .slider-controls {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    margin-top: 3rem;
  }

  .slider-btn {
    width: 50px;
    height: 50px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    color: #e5e5e5;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 300ms ease;
    backdrop-filter: blur(8px);
  }

  .slider-btn:hover {
    background: rgba(96, 165, 250, 0.1);
    border-color: rgba(96, 165, 250, 0.2);
    color: #60a5fa;
  }

  .slider-btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .slider-dots {
    display: flex;
    gap: 0.75rem;
  }

  .dot {
    width: 10px;
    height: 10px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    cursor: pointer;
    transition: all 300ms ease;
  }

  .dot.active {
    background: #60a5fa;
    transform: scale(1.2);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .cases-section {
      padding: 4rem 0;
    }

    .container {
      padding: 0 1rem;
    }

    .case-card {
      padding: 1.5rem;
    }

    .case-header {
      flex-direction: column;
      text-align: center;
      gap: 1rem;
    }

    .case-results {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .feature-tag {
      font-size: 0.75rem;
      padding: 0.375rem 0.75rem;
    }

    .slider-controls {
      gap: 1rem;
    }

    .slider-btn {
      width: 40px;
      height: 40px;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const track = document.getElementById('casesTrack');
    const prevBtn = document.getElementById('prevBtn');
    const nextBtn = document.getElementById('nextBtn');
    const dotsContainer = document.getElementById('sliderDots');
    
    if (!track || !prevBtn || !nextBtn || !dotsContainer) return;

    const cards = track.querySelectorAll('.case-card');
    let currentIndex = 0;

    // Crear dots
    cards.forEach((_, index) => {
      const dot = document.createElement('div');
      dot.className = `dot ${index === 0 ? 'active' : ''}`;
      dot.addEventListener('click', () => goToSlide(index));
      dotsContainer.appendChild(dot);
    });

    const dots = dotsContainer.querySelectorAll('.dot');

    function updateSlider() {
      track.style.transform = `translateX(-${currentIndex * 100}%)`;
      
      // Actualizar dots
      dots.forEach((dot, index) => {
        dot.classList.toggle('active', index === currentIndex);
      });

      // Actualizar botones
      prevBtn.disabled = currentIndex === 0;
      nextBtn.disabled = currentIndex === cards.length - 1;
    }

    function goToSlide(index) {
      currentIndex = Math.max(0, Math.min(index, cards.length - 1));
      updateSlider();
    }

    function nextSlide() {
      if (currentIndex < cards.length - 1) {
        goToSlide(currentIndex + 1);
      }
    }

    function prevSlide() {
      if (currentIndex > 0) {
        goToSlide(currentIndex - 1);
      }
    }

    // Event listeners
    nextBtn.addEventListener('click', nextSlide);
    prevBtn.addEventListener('click', prevSlide);

    // Autoplay
    let autoplayInterval = setInterval(nextSlide, 8000);

    // Pausar autoplay al hover
    track.addEventListener('mouseenter', () => {
      clearInterval(autoplayInterval);
    });

    track.addEventListener('mouseleave', () => {
      autoplayInterval = setInterval(nextSlide, 8000);
    });

    // Navegación con teclado
    document.addEventListener('keydown', (e) => {
      if (e.key === 'ArrowLeft') {
        prevSlide();
      } else if (e.key === 'ArrowRight') {
        nextSlide();
      }
    });

    // Touch/swipe support
    let startX = 0;
    let endX = 0;

    track.addEventListener('touchstart', (e) => {
      startX = e.touches[0].clientX;
    });

    track.addEventListener('touchend', (e) => {
      endX = e.changedTouches[0].clientX;
      const diffX = startX - endX;

      if (Math.abs(diffX) > 50) {
        if (diffX > 0) {
          nextSlide();
        } else {
          prevSlide();
        }
      }
    });

    // Inicializar
    updateSlider();

    // Intersection Observer para animaciones
    const observerOptions = {
      threshold: 0.3,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('animate-in');
        }
      });
    }, observerOptions);

    cards.forEach(card => observer.observe(card));
  });
</script>
