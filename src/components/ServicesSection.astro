---
// Componente para la sección de Servicios con cards animadas
---

<section class="services-section">
  <div class="container">
    <div class="section-header">
      <div class="section-badge">
        <span>Nuestros Servicios</span>
      </div>
      
      <h2 class="section-title reveal-text">
        Soluciones de IA que transforman tu negocio
      </h2>
      
      <p class="section-description reveal-text" data-delay="200">
        Automatizamos procesos, optimizamos recursos y potenciamos el crecimiento de tu empresa con tecnología de vanguardia.
      </p>
    </div>

    <div class="services-grid">
      <div class="service-card magnetic-element" data-delay="0">
        <div class="card-icon">
          <svg class="lottie-icon" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M12 2L2 7l10 5 10-5-10-5z"/>
            <path d="M2 17l10 5 10-5"/>
            <path d="M2 12l10 5 10-5"/>
          </svg>
        </div>
        
        <h3 class="card-title">Automatización de Procesos</h3>
        
        <p class="card-description">
          Transformamos tareas repetitivas en procesos automatizados que funcionan 24/7, liberando a tu equipo para tareas de mayor valor.
        </p>
        
        <ul class="card-features">
          <li>Workflows inteligentes</li>
          <li>Integración con sistemas existentes</li>
          <li>Monitoreo en tiempo real</li>
        </ul>
        
        <div class="card-footer">
          <span class="card-price">Desde €299/mes</span>
          <button class="card-cta">
            <span>Conocer más</span>
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
        </div>
      </div>

      <div class="service-card magnetic-element" data-delay="200">
        <div class="card-icon">
          <svg class="lottie-icon" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M9 12l2 2 4-4"/>
            <path d="M21 12c-1 0-3-1-3-3s2-3 3-3 3 1 3 3-2 3-3 3"/>
            <path d="M3 12c1 0 3-1 3-3s-2-3-3-3-3 1-3 3 2 3 3 3"/>
            <path d="M3 12v9l9-9"/>
          </svg>
        </div>
        
        <h3 class="card-title">Chatbots Inteligentes</h3>
        
        <p class="card-description">
          Desarrollamos asistentes virtuales que entienden el contexto, resuelven consultas y mejoran la experiencia del cliente.
        </p>
        
        <ul class="card-features">
          <li>IA conversacional avanzada</li>
          <li>Integración multicanal</li>
          <li>Análisis de sentimientos</li>
        </ul>
        
        <div class="card-footer">
          <span class="card-price">Desde €199/mes</span>
          <button class="card-cta">
            <span>Conocer más</span>
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
        </div>
      </div>

      <div class="service-card magnetic-element" data-delay="400">
        <div class="card-icon">
          <svg class="lottie-icon" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M3 3v18h18"/>
            <path d="M18 17V9"/>
            <path d="M13 17V5"/>
            <path d="M8 17v-3"/>
          </svg>
        </div>
        
        <h3 class="card-title">Analytics Predictivo</h3>
        
        <p class="card-description">
          Convertimos tus datos en insights accionables para tomar decisiones informadas y anticiparte a las tendencias del mercado.
        </p>
        
        <ul class="card-features">
          <li>Machine Learning aplicado</li>
          <li>Dashboards personalizados</li>
          <li>Predicciones precisas</li>
        </ul>
        
        <div class="card-footer">
          <span class="card-price">Desde €399/mes</span>
          <button class="card-cta">
            <span>Conocer más</span>
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </button>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .services-section {
    padding: 8rem 0;
    background: #171717;
    position: relative;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .section-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(96, 165, 250, 0.1);
    border: 1px solid rgba(96, 165, 250, 0.2);
    border-radius: 50px;
    color: #60a5fa;
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 2rem;
  }

  .section-title {
    font-family: 'Inter Tight', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 600;
    line-height: 1.2;
    color: #e5e5e5;
    margin-bottom: 1.5rem;
    letter-spacing: -0.01em;
  }

  .section-description {
    font-size: 1.25rem;
    line-height: 1.6;
    color: #a3a3a3;
    margin: 0;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .service-card {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(8px);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 24px;
    padding: 2rem;
    position: relative;
    transition: all 400ms cubic-bezier(0.22, 0.61, 0.36, 1);
    overflow: hidden;
    opacity: 0;
    transform: translateY(20px);
    animation: reveal-up 600ms ease forwards;
    cursor: pointer;
  }

  .service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(96, 165, 250, 0.3), transparent);
    opacity: 0;
    transition: opacity 400ms ease;
  }

  .service-card:hover {
    transform: translateY(-4px) scale(1.02);
    border-color: rgba(96, 165, 250, 0.2);
    box-shadow: 
      0 8px 25px rgba(0, 0, 0, 0.15),
      0 0 40px rgba(96, 165, 250, 0.1);
  }

  .service-card:hover::before {
    opacity: 1;
  }

  .card-icon {
    width: 64px;
    height: 64px;
    background: rgba(96, 165, 250, 0.1);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    position: relative;
  }

  .lottie-icon {
    color: #60a5fa;
    transition: transform 400ms ease;
  }

  .service-card:hover .lottie-icon {
    transform: scale(1.1) rotate(5deg);
  }

  .card-title {
    font-size: 1.5rem;
    font-weight: 600;
    color: #e5e5e5;
    margin-bottom: 1rem;
    line-height: 1.3;
  }

  .card-description {
    color: #a3a3a3;
    line-height: 1.6;
    margin-bottom: 1.5rem;
    font-size: 1rem;
  }

  .card-features {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
  }

  .card-features li {
    position: relative;
    padding-left: 1.5rem;
    color: #d4d4d4;
    font-size: 0.875rem;
    margin-bottom: 0.5rem;
  }

  .card-features li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: #60a5fa;
    font-weight: 600;
  }

  .card-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .card-price {
    font-size: 1.25rem;
    font-weight: 600;
    color: #60a5fa;
  }

  .card-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    background: transparent;
    color: #e5e5e5;
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 200ms ease;
  }

  .card-cta:hover {
    background: rgba(96, 165, 250, 0.1);
    border-color: rgba(96, 165, 250, 0.3);
    color: #60a5fa;
  }

  .card-cta svg {
    transition: transform 200ms ease;
  }

  .card-cta:hover svg {
    transform: translateX(2px);
  }

  .reveal-text {
    opacity: 0;
    transform: translateY(15px);
    transition: all 200ms ease;
  }

  .reveal-text.revealed {
    opacity: 1;
    transform: translateY(0);
  }

  @keyframes reveal-up {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .services-section {
      padding: 4rem 0;
    }

    .container {
      padding: 0 1rem;
    }

    .services-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .service-card {
      padding: 1.5rem;
    }

    .card-footer {
      flex-direction: column;
      gap: 1rem;
      align-items: flex-start;
    }

    .card-cta {
      width: 100%;
      justify-content: center;
    }
  }
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Intersection Observer para reveal animations
  const revealElements = document.querySelectorAll('.reveal-text');
  
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const delay = parseInt(entry.target.dataset.delay) || 0;
        setTimeout(() => {
          entry.target.classList.add('revealed');
        }, delay);
        revealObserver.unobserve(entry.target);
      }
    });
  }, {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  });
  
  revealElements.forEach(el => revealObserver.observe(el));

  // Animación escalonada de cards
  const serviceCards = document.querySelectorAll('.service-card');
  serviceCards.forEach((card, index) => {
    const delay = parseInt(card.dataset.delay) || 0;
    card.style.animationDelay = `${delay + 200}ms`;
  });

  // Efecto magnético en hover
  const magneticElements = document.querySelectorAll('.magnetic-element');
  
  magneticElements.forEach(element => {
    element.addEventListener('mousemove', (e) => {
      const rect = element.getBoundingClientRect();
      const x = e.clientX - rect.left - rect.width / 2;
      const y = e.clientY - rect.top - rect.height / 2;
      
      const moveX = x * 0.1;
      const moveY = y * 0.1;
      
      element.style.transform = `translate(${moveX}px, ${moveY}px) scale(1.02)`;
    });
    
    element.addEventListener('mouseleave', () => {
      element.style.transform = 'translate(0, 0) scale(1)';
    });
  });
});
</script>
