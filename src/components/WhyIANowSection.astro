---
// Sección: Por qué implementar IA ahora
---

<section id="why-ai-now" class="why-ia-section">
  <!-- Fondo con gradiente -->
  <div class="why__background">
    <div class="gradient-orb gradient-orb--1"></div>
    <div class="gradient-orb gradient-orb--2"></div>
    <div class="gradient-orb gradient-orb--3"></div>
  </div>

  <div class="container">
    <div class="why-header">
      <h2 class="why-title">
        El momento es <span class="gradient-text">AHORA</span>
      </h2>
      
      <p class="why-subtitle">
        La Inteligencia Artificial no es el futuro. Es el presente que está transformando empresas cada día.
      </p>
    </div>

    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-number">73%</div>
        <div class="stat-description">
          de empresas con IA reportan un aumento del 25% en productividad
        </div>
      </div>

      <div class="stat-card">
        <div class="stat-number">50%</div>
        <div class="stat-description">
          de trabajos administrativos desaparecerán en los próximos 2 años
        </div>
      </div>

      <div class="stat-card">
        <div class="stat-number">3x</div>
        <div class="stat-description">
          más rápido avanzan las empresas con IA vs su competencia
        </div>
      </div>
    </div>

    <div class="impact-section">
      <h3 class="impact-title">Lo que la IA hace por tu empresa <span class="highlight">HOY</span></h3>
      
      <div class="benefits-grid">
        <div class="benefit-item">
          <h4>Automatización Inteligente</h4>
          <p>Libera hasta 4 horas diarias por empleado. Reduce errores humanos en un 85%. Tu equipo se enfoca en tareas estratégicas de alto valor.</p>
        </div>

        <div class="benefit-item">
          <h4>Eficiencia Multiplicada</h4>
          <p>Procesamiento de datos 100x más rápido. Respuestas instantáneas a clientes 24/7. Análisis predictivos para decisiones más inteligentes.</p>
        </div>

        <div class="benefit-item">
          <h4>Ventaja Competitiva</h4>
          <p>Mientras dudas, tu competencia ya está implementando. Los early adopters capturan el 70% del mercado. El coste de entrada aumenta cada mes.</p>
        </div>
      </div>
    </div>

    <div class="warning-section">
      <h3 class="warning-title">La realidad que no puedes ignorar</h3>
      
      <div class="warning-content">
        <div class="warning-block">
          <h4>Para los trabajadores:</h4>
          <p>Los que dominen IA multiplicarán su valor x10. Los que la ignoren serán reemplazados en meses, no años. La adaptación HOY garantiza empleabilidad MAÑANA.</p>
        </div>

        <div class="warning-block">
          <h4>Para las empresas:</h4>
          <p>Sin IA = Sin futuro. La brecha competitiva se vuelve insalvable en 6-12 meses. Las empresas que se resistan al cambio no sobrevivirán al 2026.</p>
        </div>
      </div>
    </div>

    <div class="urgency-section">
      <h3 class="urgency-title">Cada día sin IA es:</h3>
      
      <div class="loss-grid">
        <div class="loss-item">
          <span class="loss-icon">💸</span>
          <p>Dinero perdido en tareas automatizables</p>
        </div>
        
        <div class="loss-item">
          <span class="loss-icon">⏰</span>
          <p>Talento desperdiciado en procesos obsoletos</p>
        </div>
        
        <div class="loss-item">
          <span class="loss-icon">🏃</span>
          <p>Terreno cedido a competidores más ágiles</p>
        </div>
      </div>
    </div>

    <div class="cta-section">
      <p class="cta-text">
        <strong>No se trata de si tu empresa puede permitirse implementar IA.</strong><br>
        <span class="cta-highlight">Se trata de si puede permitirse NO hacerlo.</span>
      </p>
    </div>
  </div>
</section>

<style>
  .why-ia-section {
    position: relative;
    background: #000000;
    padding: 6rem 0;
    overflow: hidden;
  }

  /* Fondo con gradientes */
  .why__background {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(120px);
    opacity: 0.2;
    will-change: transform;
  }

  .gradient-orb--1 {
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, #ef4444 0%, transparent 70%);
    top: -200px;
    right: -200px;
    animation: pulse-slow 8s ease-in-out infinite;
  }

  .gradient-orb--2 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, #f59e0b 0%, transparent 70%);
    bottom: -150px;
    left: -150px;
    animation: pulse-slow 10s ease-in-out infinite reverse;
  }

  .gradient-orb--3 {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, #60a5fa 0%, transparent 70%);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation: pulse-slow 12s ease-in-out infinite;
  }

  @keyframes pulse-slow {
    0%, 100% { transform: scale(1); opacity: 0.2; }
    50% { transform: scale(1.1); opacity: 0.3; }
  }

  .container {
    position: relative;
    z-index: 10;
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Header */
  .why-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .why-title {
    font-family: 'Inter Tight', -apple-system, sans-serif;
    font-size: clamp(3rem, 6vw, 4.5rem);
    font-weight: 800;
    line-height: 1.1;
    color: #ffffff;
    margin-bottom: 1.5rem;
    letter-spacing: -0.03em;
  }

  .gradient-text {
    background: linear-gradient(135deg, #ef4444 0%, #f59e0b 50%, #60a5fa 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .why-subtitle {
    font-size: 1.5rem;
    color: #9ca3af;
    line-height: 1.5;
    font-weight: 400;
  }

  /* Stats Grid */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-bottom: 5rem;
  }

  .stat-card {
    text-align: center;
    padding: 2rem;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    transition: all 300ms ease;
  }

  .stat-card:hover {
    transform: translateY(-5px);
    border-color: rgba(239, 68, 68, 0.3);
    background: rgba(255, 255, 255, 0.04);
  }

  .stat-number {
    font-family: 'Inter Tight', sans-serif;
    font-size: 3.5rem;
    font-weight: 800;
    background: linear-gradient(135deg, #ef4444 0%, #f59e0b 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.5rem;
  }

  .stat-description {
    color: #e5e5e5;
    font-size: 1rem;
    line-height: 1.4;
  }

  /* Impact Section */
  .impact-section {
    margin-bottom: 4rem;
  }

  .impact-title {
    font-family: 'Inter Tight', sans-serif;
    font-size: 2.5rem;
    font-weight: 700;
    color: #ffffff;
    text-align: center;
    margin-bottom: 3rem;
    line-height: 1.2;
  }

  .highlight {
    color: #60a5fa;
  }

  .benefits-grid {
    display: grid;
    gap: 3rem;
  }

  .benefit-item {
    text-align: center;
    opacity: 0;
    transform: translateY(20px);
    animation: fade-in-up 600ms ease forwards;
  }

  .benefit-item:nth-child(1) { animation-delay: 100ms; }
  .benefit-item:nth-child(2) { animation-delay: 200ms; }
  .benefit-item:nth-child(3) { animation-delay: 300ms; }

  .benefit-item h4 {
    font-family: 'Inter Tight', sans-serif;
    font-size: 1.75rem;
    font-weight: 600;
    color: #60a5fa;
    margin-bottom: 1rem;
  }

  .benefit-item p {
    color: #d1d5db;
    font-size: 1.125rem;
    line-height: 1.6;
    max-width: 600px;
    margin: 0 auto;
  }

  /* Warning Section */
  .warning-section {
    background: rgba(239, 68, 68, 0.05);
    border: 1px solid rgba(239, 68, 68, 0.2);
    border-radius: 24px;
    padding: 3rem;
    margin-bottom: 4rem;
    backdrop-filter: blur(10px);
  }

  .warning-title {
    font-family: 'Inter Tight', sans-serif;
    font-size: 2rem;
    font-weight: 700;
    color: #ef4444;
    text-align: center;
    margin-bottom: 2.5rem;
  }

  .warning-content {
    display: grid;
    gap: 2rem;
  }

  .warning-block {
    text-align: center;
  }

  .warning-block h4 {
    font-size: 1.25rem;
    font-weight: 600;
    color: #f59e0b;
    margin-bottom: 0.75rem;
  }

  .warning-block p {
    color: #e5e5e5;
    font-size: 1.1rem;
    line-height: 1.6;
  }

  /* Urgency Section */
  .urgency-section {
    margin-bottom: 4rem;
  }

  .urgency-title {
    font-family: 'Inter Tight', sans-serif;
    font-size: 2rem;
    font-weight: 700;
    color: #ffffff;
    text-align: center;
    margin-bottom: 2.5rem;
  }

  .loss-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .loss-item {
    text-align: center;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.02);
    border-radius: 16px;
    transition: all 300ms ease;
  }

  .loss-item:hover {
    background: rgba(255, 255, 255, 0.05);
    transform: translateY(-3px);
  }

  .loss-icon {
    font-size: 2.5rem;
    display: block;
    margin-bottom: 1rem;
  }

  .loss-item p {
    color: #d1d5db;
    font-size: 1rem;
    line-height: 1.4;
  }

  /* CTA Section */
  .cta-section {
    text-align: center;
    padding: 3rem;
    background: linear-gradient(135deg, rgba(96, 165, 250, 0.1) 0%, rgba(168, 85, 247, 0.1) 100%);
    border-radius: 24px;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .cta-text {
    font-size: 1.5rem;
    line-height: 1.6;
    color: #e5e5e5;
  }

  .cta-text strong {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
  }

  .cta-highlight {
    font-size: 1.75rem;
    font-weight: 700;
    background: linear-gradient(135deg, #60a5fa 0%, #a855f7 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  @keyframes fade-in-up {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .why-ia-section {
      padding: 4rem 0;
    }

    .container {
      padding: 0 1rem;
    }

    .why-title {
      font-size: 2.5rem;
    }

    .why-subtitle {
      font-size: 1.25rem;
    }

    .stats-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .stat-number {
      font-size: 2.5rem;
    }

    .impact-title {
      font-size: 1.75rem;
    }

    .benefit-item h4 {
      font-size: 1.5rem;
    }

    .benefit-item p {
      font-size: 1rem;
    }

    .warning-section {
      padding: 2rem;
    }

    .loss-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .cta-text {
      font-size: 1.25rem;
    }

    .cta-highlight {
      font-size: 1.5rem;
    }

    .gradient-orb--1,
    .gradient-orb--2,
    .gradient-orb--3 {
      width: 300px;
      height: 300px;
    }
  }

  @media (max-width: 480px) {
    .why-title {
      font-size: 2rem;
    }

    .why-subtitle {
      font-size: 1.125rem;
    }

    .stat-number {
      font-size: 2rem;
    }

    .stat-description {
      font-size: 0.9rem;
    }

    .impact-title {
      font-size: 1.5rem;
    }

    .benefit-item h4 {
      font-size: 1.25rem;
    }

    .warning-title {
      font-size: 1.5rem;
    }

    .warning-block h4 {
      font-size: 1.125rem;
    }

    .warning-block p {
      font-size: 1rem;
    }

    .urgency-title {
      font-size: 1.5rem;
    }

    .cta-text {
      font-size: 1.125rem;
    }

    .cta-highlight {
      font-size: 1.25rem;
    }
  }
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Intersection Observer para animaciones
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('in-view');
        
        // Animar números si es una stat-card
        if (entry.target.classList.contains('stat-card')) {
          const numberElement = entry.target.querySelector('.stat-number');
          if (numberElement) {
            animateNumber(numberElement);
          }
        }
      }
    });
  }, observerOptions);

  // Observar elementos
  const elements = document.querySelectorAll('.stat-card, .benefit-item, .warning-section, .loss-item');
  elements.forEach(el => observer.observe(el));

  // Función para animar números
  function animateNumber(element) {
    const finalText = element.textContent;
    const hasPercentage = finalText.includes('%');
    const hasX = finalText.includes('x');
    
    let finalNumber;
    if (hasPercentage) {
      finalNumber = parseInt(finalText.replace('%', ''));
    } else if (hasX) {
      finalNumber = parseInt(finalText.replace('x', ''));
    } else {
      finalNumber = parseInt(finalText);
    }

    let currentNumber = 0;
    const increment = finalNumber / 50;
    const timer = setInterval(() => {
      currentNumber += increment;
      if (currentNumber >= finalNumber) {
        currentNumber = finalNumber;
        clearInterval(timer);
      }
      
      let displayText = Math.floor(currentNumber).toString();
      if (hasPercentage) displayText += '%';
      if (hasX) displayText += 'x';
      
      element.textContent = displayText;
    }, 30);
  }

  // Efecto parallax suave para los gradient orbs
  const orbs = document.querySelectorAll('.gradient-orb');
  let mouseX = 0;
  let mouseY = 0;

  document.addEventListener('mousemove', (e) => {
    mouseX = e.clientX / window.innerWidth - 0.5;
    mouseY = e.clientY / window.innerHeight - 0.5;
  });

  function updateOrbPositions() {
    orbs.forEach((orb, index) => {
      const speed = (index + 1) * 0.05;
      const x = mouseX * speed * 100;
      const y = mouseY * speed * 100;
      orb.style.transform = `translate(${x}px, ${y}px)`;
    });
    requestAnimationFrame(updateOrbPositions);
  }

  updateOrbPositions();
});
</script>
</thinking>

Perfecto, he creado el nuevo componente. Ahora necesito actualizar el archivo `index.astro` para incluir esta nueva sección justo después de `ServicesIASection`:

<function_calls>
<invoke name="edit_file">
<parameter name="path">C:\Users\nestu\Desktop\🏢 Blixel\BLIXEL AI\.Astro\src\pages\index.astro